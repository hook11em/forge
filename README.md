# Forge

Forge is an internal AI venture lab and command center.

## Deploy to Cloudflare Workers

Use:

```bash
npx wrangler deploy
```

Cloudflare should detect `wrangler.jsonc` and publish the files in `/public`.
